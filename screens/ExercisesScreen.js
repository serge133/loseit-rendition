import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ExercisesScreen = props => {
  return (
    <View>
      <Text>The Exercise Screen</Text>
    </View>
  );
};

export default ExercisesScreen;

const styles = StyleSheet.create({});
